import React from 'react';
import ReactDOM from 'react-dom';
import './css/App.css'; // Changed from './index.css' to the correct path
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
